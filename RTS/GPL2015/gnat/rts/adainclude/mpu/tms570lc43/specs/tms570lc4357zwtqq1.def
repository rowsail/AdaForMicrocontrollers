/*
 * User modifiable values:
 *
 * __FLASH_START   origin of FLASH memory
 * __FLASH_SIZE    size of FLASH memory
 *
 * __SRAM_START    origin of SRAM
 * __SRAM_SIZE     size of SRAM
 *
 * __SDRAM_START   origin of SDRAM
 * __SDRAM_SIZE    size of SDRAM
 *
 * __STACK_SIZE    size of the stack to be used by the main
 *                 procedure (environment task)
 *
 */

#define __FLASH_START 0x00000000
#define __FLASH_SIZE  4096K

#define __SRAM_START  0x08000000
#define __SRAM_SIZE   512K

#define __SDRAM_START 0x60000000
#define __SDRAM_SIZE  8M

#define __STACK_SIZE  (20 * 1024)
